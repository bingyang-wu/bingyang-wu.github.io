name: Hexo

# 触发条件：在 push 到 dev 分支后
on:
  push:
    branches:
      - dev

# 任务
jobs:
  build-and-deploy:
    # 服务器环境：最新版 Ubuntu
    runs-on: ubuntu-latest
    steps:
      # 拉取代码
      - name: Checkout
        uses: actions/checkout@v1
        with:
          persist-credentials: false

      # npm install
#      - name: Install
#        run: npm install

#      # 生成静态文件
#      - name: Build
#        run:  npm run build

      - name: Use Node.js 10.x # 指定 Node.js 版本
        uses: actions/setup-node@v1
        with:
          node-version: 10.x

      - name: Setup Hexo env # 初始化 Hexo 环境
        run: |
          npm install hexo-cli -g
          npm install


      # 部署到 GitHub Pages
#      - name: Deploy
#        uses: JamesIves/github-pages-deploy-action@releases/v3
#        with:
#          ACCESS_TOKEN: ${{ secrets.GITPAGE_TOKEN }}
#          BRANCH: master
#          FOLDER: example/docs/public
